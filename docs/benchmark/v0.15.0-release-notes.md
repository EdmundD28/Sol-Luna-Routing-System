P008 adds a candidate-bound first-handoff preflight and integrates the compact boundary matrix into the Sol-Luna workflow.

- Sol-Luna showed no displayed five-hour or weekly decrease while Sol-only decreased by 2 and 1 percentage points in the same reset windows.
- The Sol-Luna route produced the stronger audited candidate and reduced same-Luna route repair from two rounds in P007 to one, without Sol replaying Luna's core implementation.
- It remained slower: 20:43 versus 12:04. One unequal-quality pair is directional evidence, not a general savings ratio.
- Diagnostic token traffic moved opposite to the included-plan dashboard and is not used as an allowance proxy in this comparison.
- Independent source review found two additional path-edge defects after the frozen route; the release candidate fixes both and passes 375 tests (one skip), the frozen referee at 10/10, and skill validation.

This is an experimental ordinary release. `v0.1.1` remains pinned as GitHub Latest because it is the user-validated baseline.
