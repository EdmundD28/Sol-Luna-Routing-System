interface:
  display_name: "Sol-Luna Delivery System"
  short_description: "Two-tier Sol control with bounded Luna execution"
  default_prompt: "Use $sol-luna to choose SOL_ONLY or SOL_LUNA, dispatch only worthwhile bounded Luna packages, and have Sol independently verify the final candidate."

policy:
  allow_implicit_invocation: false
